# adventofcode

```shell
# new day example
# run copy_template.py with args for YEAR and DAY, ex:
$ py copy_template.py 2023 1
# if the folder doesn't exists it creates it and copies the templete files
# go to folder
$ cd ./2023/1
# run with test-data
$ py main.py
# run with real data
$ py main.py --real
```